# Configurações da aplicação
BRAND_NAME="Casa de Apostas Demo"
MIN_AGE=18
APP_URL=http://localhost:3000
PORT=3000
NODE_ENV=development

# Segurança
# Gere um segredo forte (ex: openssl rand -base64 32)
SESSION_SECRET=change_this_to_a_secure_random_string
# Token para validar webhooks recebidos
WEBHOOK_TOKEN=change_this_token

# Admin Inicial (usado apenas para criar o primeiro admin via seed/startup)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!

# Pagamentos (Pix)
PIX_KEY=seu_email_pix@example.com
PIX_NAME="Nome do Recebedor"
PIX_CITY="Cidade"

# Configuração de E-mail (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=password
SMTP_FROM=no-reply@example.com

# Integrações Opcionais
RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET=

# Identity API (KYC)
IDENTITY_API_URL=
IDENTITY_API_TOKEN=

# Pluggy (Open Finance)
PLUGGY_CLIENT_ID=
PLUGGY_CLIENT_SECRET=
PLUGGY_ITEM_ID=
PLUGGY_BASE_URL=https://api.pluggy.ai

# Redis (Opcional, se for usar para sessões no futuro)
REDIS_URL=
